package com.kodilla.good.patterns.challenges.productOrderService.Segway;

import com.kodilla.good.patterns.challenges.productOrderService.User;
import com.kodilla.good.patterns.challenges.productOrderService.interfaces.RentalRepository;

import java.time.LocalDateTime;

public class SegwayRentalRepository implements RentalRepository {
    @Override
    public void createRental(User user, LocalDateTime from, LocalDateTime to) {

    }
}
